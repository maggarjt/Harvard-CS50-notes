#include <cs50.h>
#include <stdio.h>
#include <string.h>

int main(void)
{
    string names[4] = {"EMMA", "RODRIGO", "RYAN", "DAVID"};
    string numbers[4] = {"617-555-0100","617-555-0101","617-555-0102","617-555-0103"};

    for (int i = 0; 1<4; i++)
    {
        if (strcmp(names[i], "EMMA") ==0)
        {
            printf("%s\n", numbers[i]);
            return 0;
        }
    }
    printf("Not found\n");
    return 1;
}
